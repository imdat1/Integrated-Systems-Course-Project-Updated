@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using Domain
@model List<Domain.Domain.Database>

<h1>Databases</h1>
<a asp-action="Create" asp-controller="Database">Add a new Database</a>
<table class="table">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                Name
            </th>
            <th>
                Username
            </th>
            <th>
                Password
            </th>
            <th>
                Database Name
            </th>
            <th>
                Host
            </th>
            <th>
                Owner Email
            </th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@for (int i = 0; i < Model.Count(); i++)
{
    var item = Model[i];
    <tr>
        <td>@(i + 1)</td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatabaseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Host)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Owner.Email)
        </td>
        <td>
            |
            <a asp-action="SeeQuestions" asp-controller="Database" asp-route-id="@item.Id">See questions and answers</a>
        </td>
        <td>
            |
            <a asp-action="Edit" asp-controller="Database" asp-route-id="@item.Id">Edit</a>
        </td>
        <td>
            |
            <a asp-action="Delete" asp-controller="Database" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
</tbody>